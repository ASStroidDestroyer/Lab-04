echo "# My First File" > my_first_file
git add my_first_file
git commit -m "Create my_first_file in main branch"
git push origin main

#include <iostream>
using namespace std;

int main() {
    int num1 = 5, num2 = 3;
    cout << "Sum: " << num1 + num2 << endl;
    return 0;
}

#include <iostream>
using namespace std;

int main() {
    // Declare two variables to store the numbers
    double num1, num2, result;

    // Ask the user to input two numbers
    cout << "Enter the first number: ";
    cin >> num1;
    cout << "Enter the second number: ";
    cin >> num2;

    // Multiply the numbers
    result = num1 * num2;

    // Display the result
    cout << "The result of multiplication is: " << result << endl;

    return 0;
}
